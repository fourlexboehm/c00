#include <unistd.h>

void	ft_print_comb(void)
{
	char a;	
	char b;
	char c;
	a = 0;
	b = 0;
	c = 0;
	while( a < 9 && b < 9 && c < 9 )
		if 	c == 9 
				
			
	if	a !== 		
		write(1, &a, 1);
		write(1, &b, 1);
		write(1, &c, 1);
	}	
}

int	main(void)
{
	ft_print_comb();
	return (0);
}
